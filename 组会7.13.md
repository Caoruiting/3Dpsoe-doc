# 代理模型训练与数据集构建问题分析

## 实验现象
- 训练集样本数量从2000改成 1000、500 个后，**测试准确率未发生变化**。
- 这表明模型对训练集规模不敏感，可能并非从数据量中获益，而是存在其他主导因素。

## 代理模型概述
- 代理模型是一个 **几何感知的 GCN 排序打分网络**。
- 它既不是普通分类网络，也不是直接回归重投影误差的网络。
- 其目标是在给定候选姿态集合中，**排序并选出最优候选**。

## 分析与解释
### 1. 敏感性 ≠ 预测决定性
- “重投影误差对全局位置敏感” 只能说明**局部导数**关系：固定其他条件时，扰动全局位置，误差会变化。
- 但这并不等价于 “只要把全局位置加进网络，测试准确率就一定很高”。
- 训练代理模型是一个 **统计预测问题**，需要在真实数据分布下，稳定地区分候选优劣。

### 2. 全局位置的实际贡献
- 全局位置确实敏感，但在 PSO（粒子群优化）处理后的全局位置特征，对最终候选排序只提供 **中等强度的信息**。
- 它不是决定性特征，因此不能单靠它提升测试准确率。

## 可能原因：泛化差距
- 训练集准确率高，测试集准确率仅为 **0.595**，这是典型的 **泛化差距**。
- 如果模型真正学到了稳定的几何规律，测试集不应如此低。
- 很可能是模型 **过拟合或记忆了训练样本中的特定统计模式**，而非学习到普适的几何关系。
- 因此，**数据构造方式本身是主要问题之一**。

## 修改建议

### 1. 数据集构造改进
对每个 `pose_2d`，建议按以下要求重新生成候选集：

- **候选数量**：至少 **5 个** 候选姿态。
- **顺序随机**：候选顺序必须 **随机打乱**，避免最优候选总是出现在固定位置。
- **质量层次**：每组候选应包含明显的 **好、中、差** 层次，让模型学习区分。
- **Hard Negative**：每组中必须包含 **困难负例** —— 即视觉上看起来合理、但重投影误差稍差的候选，增加判别难度。

### 2. 特征输入改进
建议引入一个 **快速、固定、非 PSO 的全局对齐方法**，获取近似投影特征。

- 输入网络的特征应为 **非优化或弱优化对齐下的投影误差信息**。
- 这种特征能更好地反映原始几何关系，避免 PSO 过度优化掩盖问题，同时降低模型对特定优化路径的依赖。

---

> **总结**：当前测试准确率低的主要原因在于数据集构造不合理（缺少困难负例、顺序固定等）以及输入特征过度依赖 PSO 优化结果。按照上述建议修改后，有望提升模型的泛化能力和排序性能。
