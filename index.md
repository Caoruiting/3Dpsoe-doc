# 概述

本模型面向2D 到 3D 人体姿态提升任务，核心目标是：给定单张图像的 2D 人体姿态（pose_2d），以及一组候选 3D 人体姿态（child_pose），评估每个 3D 候选与当前 2D 姿态的匹配程度，筛选出最优的 3D 姿态结果。

模型采用条件概率建模思路，而非传统的单点数值回归：模型不直接预测单个 3D 姿态的绝对质量分数，而是学习在「当前 2D 姿态」的条件下，不同 3D 候选的相对选择概率。

# 整体架构

模型采用三段式核心结构，配合概率化损失函数完成训练：

## 核心设计逻辑

- 两个独立分支分别编码 2D 姿态和 3D 候选的骨架结构特征
- 融合网络完成跨模态特征匹配，判断 3D 候选与 2D 姿态的适配性
- 输出头输出每个候选的倾向分数，通过概率归一化得到选择概率
- 以 Boltzmann 分布构造真实目标分布，通过 KL 散度优化模型预测结果

# 核心模块详解

## 3.1 双分支 GCN 编码器

### 模块作用

两个独立的图卷积网络（GCN）分支，分别提取 2D 姿态和 3D 候选姿态的骨架结构特征。

### 分支划分

- pose_2d 分支：输入为 2D 人体姿态坐标，学习 2D 姿态的结构语义，输出特征 pose_2d_feature。
- child_pose 分支：输入为候选 3D 人体姿态坐标，学习 3D 候选的结构语义，输出特征 child_pose_feature。

### 为什么选择 GCN

人体姿态本质是图结构数据：关节点为图节点，骨骼连接为图边（如肩膀 - 手肘、髋关节 - 膝盖），存在天然的拓扑连接关系。

GCN（图卷积网络）可以更自然地利用人体骨架的结构先验，相比全连接网络更适配姿态特征提取。

## 3.2 融合网络

### 模块作用

将 2D 姿态特征与 3D 候选特征进行融合，学习两者的匹配关联，输出融合特征 fused_feature。

融合网络的核心学习目标是判断 3D 候选与 2D 姿态是否匹配，具体学习维度包括：

- 3D 姿态投影到 2D 平面后，是否与输入 2D 姿态几何一致
- 两者的身体结构、关节拓扑是否对应
- 左右手脚等对称部位是否一一匹配
- 该 3D 候选是否能合理解释当前的 2D 姿态观测
- 该候选姿态应当得到高分还是低分

## 3.3 能量分数头（Score Head）

### 模块作用

接收融合特征，输出每个 child_pose 对应的选择倾向分数（score）。

注意：该分数不等同于真实匹配度 child_fitness，是模型内部的相对排序分数，用于后续概率归一化。

# 训练策略与损失函数

## 4.1 核心设计思想

模型不采用 MSE 等损失直接拟合 fitness 的绝对数值，而是采用概率分布匹配的思路：

针对同一个 pose_2d 对应的所有 child_pose 候选，模型学习「哪个候选更可能被选中」的相对概率分布。

## 4.2 Boltzmann 目标分布（真实分布 P）

我们基于真实的 child_fitness 构造目标概率分布，步骤如下：

- 分数放缩：通过 Boltzmann 机制放大高分候选的权重、压低低分候选的权重，拉开优劣候选的差距。
- 归一化：对同一个 pose_2d 下的所有候选的 fitness 做 softmax 归一化，得到和为 1 的概率分布，即真实分布 P。
- 示例：同一个 2D 姿态对应 5 个 3D 候选，真实 fitness 经过 Boltzmann softmax 后，得到每个候选的真实被选概率。

## 4.3 模型预测分布 Q

模型前向传播得到所有候选的 score 后，对同一个 pose_2d 下的所有候选分数做 softmax 归一化，得到模型预测的概率分布 Q，即 Q(child\_pose | pose\_2d)。

该分布表示：在当前 2D 姿态的条件下，模型认为每个 3D 候选值得被选中的概率。

## 4.4 KL 散度损失（KL Loss）

### 损失定义

使用 KL 散度（Kullback-Leibler Divergence）衡量预测分布 Q 和真实分布 P 的差异，作为训练损失。

- 当模型预测分布与真实目标分布越接近，损失值越小
- 当模型把高概率分配给低 fitness 的候选时，损失值会显著增大

### 优化目标

让模型学习到的候选排序概率，与真实 fitness 对应的 Boltzmann 分布尽可能一致。

# 完整工作流程

## 5.1 推理流程（前向传播）

## 5.2 训练完整流程

# 模型核心特性

## 6.1 条件化评估

模型对 3D 姿态的质量评估不是独立判断，而是严格基于当前输入的 2D 姿态条件：

- 同一个 3D 候选，在不同的 2D 姿态下，得到的匹配分数可能完全不同。
- 模型判断的是「这个 3D 姿态和当前 2D 姿态搭不搭」，而非「这个 3D 姿态本身好不好」。

## 6.2 概率化建模

模型属于概率分布建模，而非传统的 MSE 单点回归：

- 学习目标是候选之间的相对排序，而非绝对数值拟合
- 输出结果具有概率可解释性
- 更适配「从多个候选中选最优」的任务场景

## 6.3 拓扑结构适配

基于 GCN 的骨架特征提取，天然适配人体关节的拓扑连接关系，相比扁平的全连接结构，能更高效地学习姿态的结构语义。